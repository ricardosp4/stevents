% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stevents.R
\name{stevents}
\alias{stevents}
\title{Create a spatio-temporal event pattern}
\usage{
stevents(x, y, time, crs = NA)
}
\arguments{
\item{x}{Numeric vector of x-coordinates (e.g., easting or longitude).}

\item{y}{Numeric vector of y-coordinates (e.g., northing or latitude).
Must be the same length as \code{x}.}

\item{time}{A vector of timestamps, either \code{POSIXct} or \code{Date}, the same
length as \code{x}. \code{Date} input is coerced to \code{POSIXct}.}

\item{crs}{Optional coordinate reference system, passed to \code{\link[sf:st_crs]{sf::st_crs()}}.
Can be an EPSG code (integer), a string (e.g., \code{"EPSG:25830"}), or
\code{NA} (default) when CRS is unknown.}
}
\value{
An object of class \code{stevents}, which is a list with elements
\code{data} (data.frame of x, y, time), \code{crs}, \code{bbox}, and \code{t_range}.
}
\description{
Constructs an \code{stevents} object representing a set of events located in
space and time. Events are sorted by time on construction.
}
\examples{
set.seed(1)
ev <- stevents(
  x = runif(50, 0, 100),
  y = runif(50, 0, 100),
  time = as.POSIXct("2023-01-01") + runif(50, 0, 365 * 86400)
)
print(ev)
}
