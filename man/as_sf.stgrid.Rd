% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_sf.R
\name{as_sf.stgrid}
\alias{as_sf.stgrid}
\title{Convert a space-time tessellation to an sf object}
\usage{
\method{as_sf}{stgrid}(x, ...)
}
\arguments{
\item{x}{An \code{stgrid} object.}

\item{...}{Currently unused.}
}
\value{
An \code{sf} object with columns \code{cell_id} (and \code{n_events} if
available), and a \code{POLYGON} geometry column carrying the CRS of \code{x}.
}
\description{
Extracts the spatial cells of an \code{stgrid} object as a standalone \code{sf}
polygon layer, making it compatible with the broader \code{sf} ecosystem for
visualisation, spatial joins, and further analysis. If \code{count_events()}
has already been run, the total event count per cell (summed across all
temporal bins) is attached as a column \code{n_events}.
}
\examples{
g <- stgrid(
  bbox      = c(0, 0, 100, 100),
  cell_size = 20,
  t_window  = as.POSIXct(c("2023-01-01", "2023-12-31")),
  t_step    = "1 month"
)
sf_g <- as_sf(g)
class(sf_g)
}
