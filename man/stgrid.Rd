% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stgrid.R
\name{stgrid}
\alias{stgrid}
\title{Create a space-time tessellation}
\usage{
stgrid(bbox, cell_size, t_window, t_step, crs = NA)
}
\arguments{
\item{bbox}{Numeric vector of length 4: \code{c(xmin, ymin, xmax, ymax)}.}

\item{cell_size}{Numeric scalar: spatial cell side length, in the units
of the supplied CRS.}

\item{t_window}{Length-2 vector (\code{POSIXct} or \code{Date}) giving the time
window \code{c(t_start, t_end)}.}

\item{t_step}{Character scalar passed to \code{\link[base:seq.POSIXt]{base::seq.POSIXt()}},
e.g. \code{"1 day"}, \code{"1 week"}, \code{"1 month"}.}

\item{crs}{Optional CRS, passed to \code{\link[sf:st_crs]{sf::st_crs()}}.}
}
\value{
An object of class \code{stgrid}: a list with an \code{sf} object of cells,
the vector of temporal breakpoints, and bookkeeping metadata.
}
\description{
Constructs an \code{stgrid} object: a regular tessellation of a rectangular
study region combined with a regular tessellation of time. This is the
basic space-time vector data cube structure used to aggregate or model
spatio-temporal point patterns.
}
\details{
Spatial cells are built with \code{\link[sf:st_make_grid]{sf::st_make_grid()}}. Temporal bins are
left-closed, right-open intervals (the convention also used by raster
tessellations and \code{xts}-style time series), so that an event falling
exactly on a break is assigned to the \emph{later} bin.
}
\examples{
g <- stgrid(
  bbox      = c(0, 0, 100, 100),
  cell_size = 20,
  t_window  = as.POSIXct(c("2023-01-01", "2023-12-31")),
  t_step    = "1 month"
)
print(g)
}
