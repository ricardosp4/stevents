% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hawkes_intensity.R
\name{hawkes_intensity}
\alias{hawkes_intensity}
\title{Compute the Hawkes process conditional intensity for each event}
\usage{
hawkes_intensity(events, alpha, beta, gamma)
}
\arguments{
\item{events}{An \code{stevents} object. Events must be sorted by time
(the constructor guarantees this).}

\item{alpha}{Positive numeric: branching ratio / excitation magnitude.
Controls how strongly each past event excites future ones.}

\item{beta}{Positive numeric: temporal decay rate (units: 1/second).
A value of \code{1/(7*86400)} means the excitation halves roughly every
week. Larger values = faster decay.}

\item{gamma}{Positive numeric: spatial decay rate (units: 1/metre if
coordinates are in metres). A value of \code{1/50000} means excitation
decays over ~50 km. Larger values = faster spatial decay.}
}
\value{
The \code{stevents} object with an additional column
\code{hawkes_intensity} in \code{events$data} giving the conditional
intensity contribution at each event from its entire past history.
The first event always receives a value of 0 (no past history).
}
\description{
For each event \code{i}, computes the cumulative excitation contribution
from all previous events using a separable spatio-temporal kernel
with exponential decay in both time and space:
}
\details{
\deqn{\Lambda_i = \sum_{j: t_j < t_i} \alpha \cdot e^{-\beta (t_i - t_j)}
  \cdot e^{-\gamma \, d_{ij}}}

where \eqn{d_{ij}} is the Euclidean distance between events \eqn{i}
and \eqn{j}. The result approximates the self-excitation component of
the Hawkes intensity at each observed event location and time.
}
\examples{
set.seed(42)
ev <- stevents(
  x    = runif(100, 0, 100),
  y    = runif(100, 0, 100),
  time = as.POSIXct("2023-01-01") + sort(runif(100, 0, 90 * 86400))
)
ev2 <- hawkes_intensity(ev, alpha = 0.5, beta = 1 / (7 * 86400), gamma = 1 / 30)
summary(ev2$data$hawkes_intensity)
}
