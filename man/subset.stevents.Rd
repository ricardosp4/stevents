% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_stevents.R
\name{subset.stevents}
\alias{subset.stevents}
\title{Subset a spatio-temporal event pattern}
\usage{
\method{subset}{stevents}(x, t_from = NULL, t_to = NULL, bbox = NULL, ...)
}
\arguments{
\item{x}{An \code{stevents} object.}

\item{t_from}{Optional. Start of temporal window (\code{POSIXct} or \code{Date},
inclusive). If \code{NULL}, no lower time bound is applied.}

\item{t_to}{Optional. End of temporal window (\code{POSIXct} or \code{Date},
inclusive). If \code{NULL}, no upper time bound is applied.}

\item{bbox}{Optional. Numeric vector of length 4
\code{c(xmin, ymin, xmax, ymax)} for spatial filtering. If \code{NULL}, no
spatial filter is applied.}

\item{...}{Currently unused.}
}
\value{
A new \code{stevents} object with the filtered events.
}
\description{
Filters an \code{stevents} object by a temporal window, a spatial bounding
box, or both. Returns a new \code{stevents} object containing only the
events that satisfy all supplied constraints.
}
\examples{
set.seed(1)
ev <- stevents(
  x    = runif(100, 0, 100),
  y    = runif(100, 0, 100),
  time = as.POSIXct("2023-01-01") + runif(100, 0, 365 * 86400)
)

# Filter by time window
ev_summer <- subset(ev,
  t_from = as.POSIXct("2023-06-01"),
  t_to   = as.POSIXct("2023-08-31")
)

# Filter by bounding box
ev_nw <- subset(ev, bbox = c(0, 50, 50, 100))

# Both at once
ev_sub <- subset(ev,
  t_from = as.POSIXct("2023-06-01"),
  t_to   = as.POSIXct("2023-08-31"),
  bbox   = c(0, 50, 50, 100)
)
}
