% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbor_history.R
\name{neighbor_history}
\alias{neighbor_history}
\title{Compute recent spatial neighbour counts for each event}
\usage{
neighbor_history(events, radius, lag)
}
\arguments{
\item{events}{An \code{stevents} object. Events must already be sorted by
time (the constructor guarantees this).}

\item{radius}{Positive numeric: spatial search radius, in the same units
as the coordinates.}

\item{lag}{Look-back window. Either a \link{difftime} object or a positive
numeric scalar interpreted as \strong{days}.}
}
\value{
The \code{stevents} object with an additional column \code{n_recent_neighbors}
appended to \code{events$data}.
}
\description{
For each event \code{i}, counts how many \emph{previous} events occurred within
spatial radius \code{radius} and within the time lag \verb{[t_i - lag, t_i)}.
This is the simplest version of the Hawkes "excitation history" feature:
a high count suggests the current event may have been triggered by
recent nearby activity.
}
\examples{
set.seed(42)
ev <- stevents(
  x    = runif(100, 0, 100),
  y    = runif(100, 0, 100),
  time = as.POSIXct("2023-01-01") + sort(runif(100, 0, 30 * 86400))
)
ev2 <- neighbor_history(ev, radius = 10, lag = 7)   # 7 days
summary(ev2$data$n_recent_neighbors)
}
