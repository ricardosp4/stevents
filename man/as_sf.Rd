% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_sf.R
\name{as_sf}
\alias{as_sf}
\title{Convert a spatio-temporal event pattern to an sf object}
\usage{
as_sf(x, ...)
}
\arguments{
\item{x}{An \code{stevents} object.}

\item{...}{Currently unused.}
}
\value{
An \code{sf} object with columns \code{time} and \code{n_recent_neighbors}
(if present), and a \code{POINT} geometry column carrying the CRS of \code{x}.
}
\description{
Converts an \code{stevents} object to an \code{sf} point layer, making it
compatible with the broader \code{sf} ecosystem for visualisation,
spatial joins, and further analysis.
}
\examples{
set.seed(1)
ev <- stevents(
  x    = runif(50, 0, 100),
  y    = runif(50, 0, 100),
  time = as.POSIXct("2023-01-01") + runif(50, 0, 365 * 86400)
)
sf_ev <- as_sf(ev)
class(sf_ev)
}
